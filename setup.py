from setuptools import setup


setup(
        name='layervis',
        author='Simon Walker',
        version='0.0.1',
        py_modules=['layervis'],
        install_requires=[
            'keras',
            'numpy',
            'matplotlib',
            ]
        )
